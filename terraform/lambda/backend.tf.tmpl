terraform {
  backend "s3" {
    bucket         = "terraform-state"
    key            = "int_framework/lambda.tfstate"
    region         = "ca-central-1"
    encrypt        = true
    dynamodb_table = "${environment}-terraform-state-lock"
  }
  required_version = ">= 1.6.4"
}
